---
 - name: Variable with loop
   hosts: all
   vars_files:
     user_list.yml

   tasks: 
     - name: create a user
       user: 
         name: "{{ item }}"
         state: present

       loop: "{{ users }}" 
