---
 - name: Simple loop example
   hosts: all

   tasks: 
     - name: remove packages
       yum:
         name: "{{ item }}"
         state: absent
       loop:
         - httpd
         - mariadb-server
         - ftp

